== [todo] Klassenbeschreibung
=== Topic `+Stammdaten+`
Das Topic `+Stammdaten+` umfasst alle statischen Werte. Darunter fallen z.B. die Aufzählwerte von Listen (INTERLIS-Datentyp «Aufzählung»). Jede Liste wird in einer eigenen Klasse modelliert.

Die Stammdaten werden durch die zuständige Stelle vorgegeben. Bei Bundesthemen ist dies das für die Modellierung zuständige Bundesamt. Bei kantonalen Themen das zuständige kantonale Amt. Die Stammdaten von Bundesthemen können auf der Stufe Kanton erweitert werden. Die kantonalen Erweiterungen werden bei der Überführung ins Bundesmodell den entsprechenden Bundestypen zugeordnet. Bei Bedarf werden die Stammdaten durch die Abteilung Geoinformation nachgeführt und im https://github.com/ch-sz-geo/<Themennummer>/tree/main/data[data-Verzeichnis] des jeweiligen Themas veröffentlicht.

==== Klasse `+Katalogeintrag+`
Die Klasse `+Katalogeintrag+` enthält die allgemeinen, für alle Kataloge gemeinsamen Attribute. Die Klasse selber ist abstrakt: Es gibt keine Objekte `+Katalogeintrag+`, sondern nur Objekte von den spezialisierten Klassen.

[cols="10%, 60%, 20%, 10%",options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
m| OID
| technischer Objektidentifikator
| ceaa37a9-8614-43fc-9a8b-688f95c30218
| öffentlich
m| Name
| Bezeichnung des Katalogeintrages, wie er den Nutzenden angezeigt wird
| in Änderung
| öffentlich
m| Code
| Code des Listeneintrages; entspricht in INTERLIS dem Wert der Aufzählung und muss ein gültiger INTERLIS-Name sein (siehe https://www.interlis.ch/dokumentation[INTERLIS-Referenzhandbuch])
| in_Aenderung
| öffentlich
m| SortierNr
| Reihenfolge des Katalogeintrages in der Auswahlliste
| 1
| öffentlich
m| Bemerkung
| Erläuterung, welche den Katalogeintrag näher beschreibt
| Dieser Status wird für alle Objekte verwendet, bei denen aktuell eine Nachführung läuft.
| öffentlich
|===

==== Klasse `+Prozesstyp+`
Die Klasse `+Prozesstyp+` führt die Werte des Katalogs "Prozesstyp". Sie ist eine Spezialisierung der Klasse Katalogeintrag.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Anforderungen*
m|Code
3+| Die Werte müssen eindeutig sein
m|Name
3+| Die Werte müssen eindeutig sein
|===

==== Klasse `+Aggregationsstufe+`
Die Klasse `+Aggregationsstufe+` führt die Werte des Katalogs "Aggregationsstufe". Sie ist eine Spezialisierung der Klasse Katalogeintrag.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Anforderungen*
m|Code
3+| Die Werte müssen eindeutig sein
m|Name
3+| Die Werte müssen eindeutig sein
|===

==== Klasse `+Flaechentyp+`
Die Klasse `+Flaechentyp+` führt die Werte des Katalogs "Flaechentyp". Sie ist eine Spezialisierung der Klasse Katalogeintrag.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Anforderungen*
m|Code
3+| Die Werte müssen eindeutig sein
m|Name
3+| Die Werte müssen eindeutig sein
|===

==== Klasse `+NutzungsklasseWirkungsraum+`
Die Klasse `+NutzungsklasseWirkungsraum+` führt die Werte des Katalogs "Nutzungsklasse des Wirkungsraums". Sie ist eine Spezialisierung der Klasse Katalogeintrag.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Anforderungen*
m|Code
3+| Die Werte müssen eindeutig sein
m|Name
3+| Die Werte müssen eindeutig sein
|===

==== Klasse `+NutzungsklasseAusgleichsraum+`
Die Klasse `+NutzungsklasseAusgleichsraum+` führt die Werte des Katalogs "Nutzungsklasse des Ausgleichsraums". Sie ist eine Spezialisierung der Klasse Katalogeintrag.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Anforderungen*
m|Code
3+| Die Werte müssen eindeutig sein
m|Name
3+| Die Werte müssen eindeutig sein
|===

==== Klasse `+NutzungsklasseReferenzraum+`
Die Klasse `+NutzungsklasseReferenzraum+` führt die Werte des Katalogs "Nutzungsklasse des Referenzraums". Sie ist eine Spezialisierung der Klasse Katalogeintrag.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Anforderungen*
m|Code
3+| Die Werte müssen eindeutig sein
m|Name
3+| Die Werte müssen eindeutig sein
|===

=== [todo] Topic `+Fachdaten+`
[todo] Das Topic `+Fachdaten+` umfasst die eigentlichen, fachlichen Klassen des Themas.

==== Klasse `+Kaltluftprozess+`
Die Klasse `+Kaltluftprozess+` führt die Informationen zum Kaltluftporzess.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
m| OID
| technischer Objektidentifikator
| ceaa37a9-8614-43fc-9a8b-688f95c30218
| öffentlich
m| erfasstVon
| Loginname der Person, welche den Datensatz erstellt hat (Autor); wird durch das System gesetzt
| Musterha
| +++<span style="color:red;">intern</span>+++
m| erfasstAm
| Datum und Zeit, an dem der Datensatz erstellt wurde; wird durch das System gesetzt
| 1980-03-21T15:38:12
| öffentlich
m| geaendertVon
| Loginname der Person, welche den Datensatz zuletzt geändert hat (Editor); wird durch das System gesetzt
| Muelleran
| +++<span style="color:red;">intern</span>+++
m| geaendertAm
| Datum und Zeit, an dem der Datensatz zuletzt geändert wurde; wird durch das System gesetzt
| 2024-07-30T08:07:57
| öffentlich
m| Windrichtung
| Windrichtung in Grad; Attribut "WR" aus Original-Datenquelle
| 195.7
| öffentlich
m| Beschreibung
| Beschreibung zum Kaltluftprozess; Attribut "Beschreibung" aus Original-Datenquelle
| Das ist eine Beschreibung
| öffentlich
4+| *Beziehungsattribute*
m| rProzesstyp
| Fremdschlüssel zum Katalog, welcher mit dem Objekt verknüpft ist.
| ccee2bad-419e-454e-9e0f-9ef2ae2d4d44
| öffentlich
4+| *Geometrie*
m| Geometrie
| Geometrie des Objektes als Startpunkt des Windrichtungspfeiles
| (ohne Beispiel)
| öffentlich
|===

==== Klasse `+Windfeld+`
Die Klasse `+Windfeld+` führt die Informationen zum Windfeld.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
m| OID
| technischer Objektidentifikator
| ceaa37a9-8614-43fc-9a8b-688f95c30218
| öffentlich
m| erfasstVon
| Loginname der Person, welche den Datensatz erstellt hat (Autor); wird durch das System gesetzt
| Musterha
| +++<span style="color:red;">intern</span>+++
m| erfasstAm
| Datum und Zeit, an dem der Datensatz erstellt wurde; wird durch das System gesetzt
| 1980-03-21T15:38:12
| öffentlich
m| geaendertVon
| Loginname der Person, welche den Datensatz zuletzt geändert hat (Editor); wird durch das System gesetzt
| Muelleran
| +++<span style="color:red;">intern</span>+++
m| geaendertAm
| Datum und Zeit, an dem der Datensatz zuletzt geändert wurde; wird durch das System gesetzt
| 2024-07-30T08:07:57
| öffentlich
m| Windrichtung
| Windrichtung in Grad; Attribut "WR" aus Original-Datenquelle
| 195.7
| öffentlich
m| Geschwindigkeit
| Windgeschwindigkeit in [m/s]; Attribut "WG" aus Original-Datenquelle
| 16.7
| öffentlich
m| uWert
| zonaler Wind (Ost-West-Richtung), Positivwert = Ostwind / Negativwert = Westwind; ergibt Windgeschwindigkeit nach Formel (u^2 + v^2)^1/2; Attribut "u" aus Original-Datenquelle
| 16.7
| öffentlich
m| vWert
| meridionaler Wind (Nord-Süd-Richtung), Positivwert = Nordwind / Negativwert = Südwind; ergibt Windgeschwindigkeit nach Formel (u^2 + v^2)^1/2; Attribut "v" aus Original-Datenquelle
| 16.7
| öffentlich
4+| *Beziehungsattribute*
m| rAggregationsstufe
| Fremdschlüssel zum Katalog, welcher mit dem Objekt verknüpft ist.
| ccee2bad-419e-454e-9e0f-9ef2ae2d4d44
| öffentlich
4+| *Geometrie*
m| Geometrie
| Geometrie des Objektes als Startpunkt des Windfeldpfeiles
| (ohne Beispiel)
| öffentlich
|===

==== Klasse `+Basisgeometrie+`
Die Klasse `+Basisgeometrie+` führt die allgemeinen Informationen zur Geometrie. Sie ist eine abstrakte Klasse und führt selbrer keine Objekte.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
m| OID
| technischer Objektidentifikator
| ceaa37a9-8614-43fc-9a8b-688f95c30218
| öffentlich
m| erfasstVon
| Loginname der Person, welche den Datensatz erstellt hat (Autor); wird durch das System gesetzt
| Musterha
| +++<span style="color:red;">intern</span>+++
m| erfasstAm
| Datum und Zeit, an dem der Datensatz erstellt wurde; wird durch das System gesetzt
| 1980-03-21T15:38:12
| öffentlich
m| geaendertVon
| Loginname der Person, welche den Datensatz zuletzt geändert hat (Editor); wird durch das System gesetzt
| Muelleran
| +++<span style="color:red;">intern</span>+++
m| geaendertAm
| Datum und Zeit, an dem der Datensatz zuletzt geändert wurde; wird durch das System gesetzt
| 2024-07-30T08:07:57
| öffentlich
m| TemperaturNacht
| mittlere Temperatur um 04:00 Uhr morgens; Attribut "t04" aus Original-Datenquelle
| 14.2
| öffentlich
m| WindgeschwindigkeitNacht
| mittlere Windgeschwindigkeit um 04:00 Uhr morgens; Attribut "wg" aus Original-Datenquelle
| 11.5
| öffentlich
m| KaltlufvolumenstromNacht
| mittlerer Kaltlufvolumenstrom um 04:00 Uhr morgens; Attribut "kvs" aus Original-Datenque
| 16.7
| öffentlich
m| KaltluftproduktionsrateNacht
| mittlerer Kaltlufvolumenstrom um 04:00 Uhr morgens; Attribut "kpr" aus Original-Datenque
| 2.8
| öffentlich
m| PET
| physiologische, equivalente Temperatur; Attribut "pet" aus Original-Datenque
| 2.8
| öffentlich
4+| *Beziehungsattribute*
m| rFlaechentyp
| Fremdschlüssel zum Katalog, welcher mit dem Objekt verknüpft ist.
| ccee2bad-419e-454e-9e0f-9ef2ae2d4d44
| öffentlich
4+| *Geometrie*
m| Geometrie
| Geometrie des Objektes als Fläche
| (ohne Beispiel)
| öffentlich
|===

==== Klasse `+Referenzraum+`
Die Klasse `+Referenzraum+` führt keine spezifischen Attribute. Sie ist eine Spezialisierung der Klasse `+Basisgeometrie+`.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Beziehungsattribute*
m| rNutzungsklasseReferenzraum
| Fremdschlüssel zum Katalog, welcher mit dem Objekt verknüpft ist.
| ccee2bad-419e-454e-9e0f-9ef2ae2d4d44
| öffentlich
4+| *Anforderungen*
m|Code
3+| Die Werte müssen eindeutig sein
m|Name
3+| Die Werte müssen eindeutig sein
|===

==== Klasse `+Bewertungsraum+`
Die Klasse `+Bewertungsraum+` führt die allgemeinen Informationen zur Bewertung. Sie ist eine abstrakte Klasse und führt selbrer keine Objekte. Sie ist eine Spezialisierung der Klasse `+Basisgeometrie+`.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
m| Tag
| [todo]
| [todo]
| öffentlich
m| Nacht
| [todo]
| [todo]
| +++<span style="color:red;">intern</span>+++
m| Planungshinweis
| [todo]
| [todo]
| öffentlich
|===

==== Klasse `+Wirkungsraum+`
Die Klasse `+Wirkungsraum+` führt die Objekte des Wirkungsraumes. Sie ist eine Spezialisierung der Klasse `+Bewertungsraum+` und enthält keine weiteren Attribute.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Beziehungsattribute*
m| rNutzungsklasseWirkungsraum
| Fremdschlüssel zum Katalog, welcher mit dem Objekt verknüpft ist.
| ccee2bad-419e-454e-9e0f-9ef2ae2d4d44
| öffentlich
4+| *Anforderungen*
4+| _keine besonderen_
|===

==== Klasse `+Ausgleichsraum+`
Die Klasse `+Ausgleichsraum+` führt die Objekte des Ausgleichsraumes. Sie ist eine Spezialisierung der Klasse `+Bewertungsraum+` und enthält keine weiteren Attribute.

[cols=4*,options="header"]
|===
| Name | Beschreibung | Beispiel | Freigabe
4+| _Diese Klasse führt keine zusätzlichen Attribute_
4+| *Beziehungsattribute*
m| rNutzungsklasseAusgleichsraum
| Fremdschlüssel zum Katalog, welcher mit dem Objekt verknüpft ist.
| ccee2bad-419e-454e-9e0f-9ef2ae2d4d44
| öffentlich
4+| *Anforderungen*
4+| _keine besonderen_
|===

ifdef::backend-pdf[]
<<<
endif::[]
